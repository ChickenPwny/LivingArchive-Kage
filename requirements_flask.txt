# Flask dependencies
Flask>=3.0.0
flask-cors>=4.0.0
Flask-WTF>=1.2.0  # CSRF protection
Flask-Limiter>=3.5.0  # Rate limiting

# Core dependencies
requests>=2.31.0
setproctitle>=1.3.0
python3-nmap>=1.9.0  # Provides nmap3 module

# Agentic AI Extension (LivingArchive-clean integration)
# Note: LivingArchive-clean gateway must be running separately
# No direct dependencies - uses HTTP API

# Environment variables (optional - for .env file support)
python-dotenv>=1.0.0

# Development dependencies (optional)
# pytest>=7.4.0
# black>=23.0.0

