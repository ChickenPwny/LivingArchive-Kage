# Core dependencies
requests>=2.31.0
setproctitle>=1.3.0
python3-nmap>=1.9.0  # Provides nmap3 module

# Database (optional - only needed if connecting to PostgreSQL)
psycopg2-binary>=2.9.0

# Environment variables (optional - for .env file support)
python-dotenv>=1.0.0

# Optional dependencies (for full functionality)
# django>=4.2.0  # Only needed if running with Django API
# sqlalchemy>=2.0.0  # Only needed for learning service

# Development dependencies
# pytest>=7.4.0
# black>=23.0.0

